--- phodevi_system_patched.php	2018-05-16 15:32:26.516676773 +0300
+++ phodevi_system.php	2018-04-16 14:46:53.000000000 +0300
@@ -75,7 +75,7 @@
 	{
 		$layer = null;
 
-		if((((phodevi::is_windows() && pts_client::executable_in_path('winecfg.exe')) || (phodevi::is_linux() && pts_client::executable_in_path('winecfg')))) && ($wine = phodevi::read_property('system', 'wine-version')))
+		if(phodevi::is_windows() && pts_client::executable_in_path('winecfg.exe') && ($wine = phodevi::read_property('system', 'wine-version')))
 		{
 			$layer = $wine;
 		}
